apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: providers
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/sumansrivastava/gitops-lab.git
    targetRevision: main
    path: gitopslab/platform/providers

  destination:
    server: https://kubernetes.default.svc
    namespace: crossplane-system

  syncPolicy:
    automated:
      prune: true
      selfHeal: true