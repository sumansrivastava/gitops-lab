apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: provider-configs
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/sumansrivastava/gitops-lab.git
    targetRevision: main
    path: gitopslab/platform/provider-configs
  

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true